# AI Hiring Funnel — 95% автоотсев

> Третий трек найма (в дополнение к EXPRESS и FULL из hiring-playbook.md).
> Используется для AI-first ролей: Growth-маркетолог, Data-аналитик, Product-менеджер, AI-инженер.
> Основан на сессии стратегии 2026-04-04. Референс-реализация: `/Users/alex/Projects/platform/specs/ROADMAP-marketer-hiring-2026-04-04.md`

---

## Зачем этот трек

Классический EXPRESS/FULL не подходит когда:
- Роль требует AI-native мышления (нужно проверить глубину работы с AI, а не просто "пользуется ChatGPT")
- У owner ограниченное время на интервью (≤ 5 часов на всех кандидатов)
- Нужен массовый отсев с сохранением качества сигнала
- Можно построить инструмент скрининга своими руками (Claude API, Telegram-бот, веб-форма)

**Принцип:** до owner доходят только 2-3 финалиста из 100 откликов. Все остальные этапы — автоматизированы через AI.

---

## Воронка из 4 этапов

```
ОТКЛИКИ (100)
  ↓  Этап 1: Telegram-бот (AI-скрининг заявки)
ПРОШЛИ АНКЕТУ (30)                         ← отсев 70%
  ↓  Этап 2: Аналитическое тестовое (задача на мышление)
СДАЛИ ТЕСТОВОЕ (10)                        ← отсев 67%
  ↓  Этап 3: AI-видеоинтервью (6-8 вопросов без подготовки)
ПРОШЛИ ВИДЕО (3-5)                         ← отсев 50-70%
  ↓  Этап 4: Живое интервью с owner
ФИНАЛИСТЫ (1-2)                            ← оффер
```

Каждый этап даёт независимый сигнал, решение принимается AI + owner в точке эскалации.

---

## Этап 1 — Telegram-бот (вместо анкеты hh.ru)

**Зачем:** собрать структурированные данные + DISC-профиль + ссылки на кейсы + оценить релевантность автоматически.

**Что делает бот:**
1. Приветствие + ссылка на полный текст вакансии
2. Спрашивает 5-7 обязательных вопросов (не поверхностных, см. ниже)
3. Просит прислать ссылки на 2-3 реальных кейса с цифрами
4. DISC-опросник (16 пунктов) встроенный в диалог
5. Claude-скоринг кейсов на релевантность по rubric
6. Прошёл → ссылка на тестовое (Этап 2). Не прошёл → вежливый отказ

**Правильные вопросы (не поверхностные):**
- Для AI-first роли: «Пришли ссылку на твой проект/агент в Claude или GPT, который ты довёл до результата. Объясни в 3 предложениях: вход, что делает, выход.»
- Для data-driven роли: «Опиши кейс, где ты принимал решение против интуиции на основе данных. Какие данные были? К чему пришёл?»
- Для скорости: «Сколько времени занимает у тебя запуск нового email-флоу с нуля? Опиши этапы.»
- Для кастдевов: «Сколько custdev-интервью ты провёл за последние 3 месяца? Что самое неожиданное услышал?»

**Запрещены вопросы типа:**
- «Какой AI используешь?» — все скажут ChatGPT
- «Расскажи о себе» — заученный ответ
- «Почему хочешь у нас работать?» — подготовленный ответ

**Хранение:** таблицы `hr_candidates`, `hr_answers`, `hr_scores` в bot_admin DB (см. ROADMAP референс).

**Инструменты:** Claude Agent SDK + aiogram + bot_admin DB. Не нужны внешние ATS (Talantix, Huntflow) — свой бот гибче.

---

## Этап 2 — Аналитическое тестовое (задача на мышление)

**Принцип:** тестовое проверяет КАК думает кандидат, а не ЧТО он знает. Формат — задача с данными и противоречиями.

### Антипаттерн

Мусорное тестовое: «Напиши план email-маркетинга для Vedantica на 3 месяца. Какие 3 гипотезы проверишь?»

Почему мусор:
- Предсказуемо — все напишут одно и то же (персонализация, A/B, реактивация)
- Не проверяет приоритизацию
- Не проверяет умение читать данные
- Не проверяет осознание ограничений

### Правильный формат

**Даём датасет** с противоречиями и ограничениями. Спрашиваем **выбор + обоснование**.

Пример для Growth-маркетолога:

```
ДАННЫЕ (таблица сегментов базы):
| Сегмент | Размер | OR | CTR | CR→заказ | Ср.чек | Последний запуск |
|---|---|---|---|---|---|---|
| Купили A, не купили B | 1200 | 22% | 4.1% | 2.8% | 88К | 4 мес назад |
| Регистрация без покупки | 18000 | 8% | 1.2% | 0.3% | 42К | 2 нед назад |
| Купили 1 курс <30К | 2400 | 18% | 3.5% | 1.9% | 35К | 6 мес назад |
| Только TG, нет email | 8500 | — | 2.8% | 0.5% | 50К | 1 мес назад |
| Ученики 2023 года | 900 | 25% | 5.2% | 0.4% | 62К | 8 мес назад |

ОГРАНИЧЕНИЯ:
- Бюджет: 0 руб. (только текущие каналы)
- Время: 2 недели (один спринт)
- Есть: Claude, GetCourse, email, TG-бот, менеджер на обзвон (15 звонков/день × 2 недели = 150)
- Цель: максимум квалифицированных заказов

ЗАДАНИЕ:
1. Выбери 2 сегмента для работы. Объясни почему именно эти, а не другие.
2. Для каждого: что запускаешь, в каком порядке, с каким прогнозом (цифры).
3. Какие данные тебе НЕ хватает? Что бы ты запросил?
4. Как распределишь 150 звонков менеджера между сегментами?
```

### Что это выявляет

| Критерий | Хороший ответ | Слабый ответ |
|---|---|---|
| Приоритизация | Выбирает по expected value (размер × CR × чек) | Выбирает "потому что интересно" |
| Причинно-следственные связи | Замечает противоречие: "ученики 2023" — высокий OR, низкий CR — 8 мес молчания, оффер нерелевантен | Игнорирует аномалии |
| Data literacy | Считает прогноз в цифрах: 1200 × 22% × 4.1% × 2.8% = ~3 заказа | Даёт только общие слова |
| Осознание ограничений | Спрашивает недостающие данные (какой был оффер, когда последний контакт) | Не задаёт уточняющих вопросов |
| Ресурсное мышление | Распределяет 150 звонков по expected value | Распределяет поровну или по размеру |

### Rubric AI-скоринга

```
ПРОМПТ ДЛЯ CLAUDE:
Ты — эксперт по найму growth-маркетологов. Оцени ответ кандидата на тестовое задание.

Датасет: [датасет]
Задание: [задание]
Ответ кандидата: [ответ]

Оцени по 5-балльной шкале по каждому критерию:
1. Приоритизация (выбирает по expected value)
2. Причинно-следственные связи (замечает аномалии)
3. Data literacy (считает в цифрах)
4. Осознание ограничений (задаёт уточняющие вопросы)
5. Ресурсное мышление (распределяет 150 звонков осмысленно)

ИТОГ: средний балл.
Порог: ≥ 3.5 из 5 = проход на Этап 3.

Формат: JSON.
```

### Таймбокс

60 минут максимум. Дедлайн — 48 часов с момента отправки. Кто не сдал = автоотказ (это сам по себе фильтр, 30-40% отвалятся).

### Антипаттерн оформления

**Не тратить кандидата** на написание документации, красивых слайдов, презентаций. Только суть — текстовый ответ в Google Doc. Оформление не оценивается.

---

## Этап 3 — AI-видеоинтервью (6-8 вопросов без подготовки)

**Зачем:** отсечь тех, кто "красиво пишет", но медленно думает. На живой камере не подготовишься к неизвестным вопросам.

### Архитектура

```
Кандидат получает персональную ссылку → /hr/interview/:token
  ↓
Веб-страница: включает камеру, видит вопрос #1
  ↓
MediaRecorder API записывает ответ (таймер 2 минуты)
  ↓
После ответа — следующий вопрос (6-8 штук, кандидат не знает сколько)
  ↓
Видео загружаются на S3 (Timeweb bucket)
  ↓
Whisper транскрипция на сервере
  ↓
Claude анализ по rubric
  ↓
Сводка + оценка в recruiter cockpit
```

**Своими руками**, не сервисами (Hireflix, VCV). Причина: интегрируется с нашим botadmin, хранится у нас, можно дорабатывать rubric.

### Вопросы (пример для маркетолога)

1. **Проект** — «Покажи мне последний проект или агент, который ты построил в Claude или GPT. Что было на входе, что на выходе?»
2. **Диагностика** — «OR email-базы упал с 18% до 9% за 3 месяца. Назови 3 возможные причины и что проверишь первым.»
3. **Действие** — «Ты видишь: сегмент 1200 человек с чеком 88К не получал писем 4 месяца. Что сделаешь в первые 48 часов?»
4. **Культурный фит** — «Что тебе интересно в теме [ниша компании]? Если ничего — скажи честно, это не дисквалифицирует.»
5. **Рабочий ритм** — «Опиши свой идеальный рабочий день: что делаешь, сколько, когда останавливаешься.»
6. **Приоритизация** — «У тебя 3 срочные задачи одновременно. Как выбираешь что делать первым?»
7. **Кастдевы** — «Как часто ты общаешься с живыми клиентами? Что обычно спрашиваешь?»
8. **Самокритика** — «Какой твой последний проваленный эксперимент? Что понял?»

### Rubric видеоинтервью

| Критерий | 1 балл | 3 балла | 5 баллов |
|---|---|---|---|
| Конкретика | Общие слова, абстракции | Есть примеры | Цифры, даты, имена, факты |
| Скорость мысли | Долгие паузы, путается | Думает 5-10 сек → отвечает | Отвечает сразу по существу |
| Глубина | Поверхностный ответ | Есть структура | Несколько уровней причинно-следственных связей |
| Уверенность | Много "ну это", "наверное" | Спокойно отвечает | Говорит как эксперт |
| Релевантность | Ушёл от темы | Частично ответил | Попал точно |

### Промпт AI-анализа

```
Ты — эксперт по найму. Транскрипт видеоинтервью кандидата ниже.

Вопрос: [вопрос]
Ответ кандидата: [транскрипт]

Оцени по 5 критериям (1-5): конкретика, скорость мысли, глубина, уверенность, релевантность.
Укажи 2 сильных сигнала и 2 слабых сигнала.
Итоговый балл: среднее.

Формат: JSON.
```

---

## Этап 4 — Живое интервью с owner (только финалисты)

**До этого этапа доходят только 2-3 кандидата из 100 откликов.** Owner получает:
- Все ответы на этапе 1
- Тестовое + AI-оценка
- Транскрипты + видео этапа 3
- AI-саммари: сильные стороны, слабые, red flags

**Формат интервью:** 30 минут, 5 вопросов с rubric, подтверждение chemistry и culture fit.

Живое интервью — **не про проверку компетенций** (уже проверены), а про:
1. Химию с owner
2. Культурный фит в команде
3. Ответы на вопросы кандидата (closing)
4. Озвучивание оффера и условий

### 5 вопросов для финального интервью

1. «Расскажи про последний провал в работе. Что именно пошло не так? Что понял?»
2. «Что тебе сейчас важно в следующей работе? В каком порядке приоритетов?»
3. «Был ли у тебя опыт, когда руководитель принимал решение, с которым ты не был согласен? Что сделал?»
4. «Если бы ты пришёл к нам и через 90 дней не выполнил план — что это, по-твоему, означало бы?»
5. «У тебя остались вопросы к нам?»

### Rubric финала

| Критерий | Стоп-сигналы | Зелёные сигналы |
|---|---|---|
| Провалы | "Не помню", "всё было хорошо" | Называет конкретный, видит свою роль |
| Приоритеты | Деньги первыми или только деньги | Задача, команда, рост — деньги во 2-3 очереди |
| Работа с руководителем | "Всегда соглашался" или "уходил" | Говорил напрямую, обосновывал |
| Взгляд на провал | Обвиняет обстоятельства | Берёт ответственность, даёт план |
| Вопросы к нам | Нет вопросов или только про ЗП | Вопросы про продукт, команду, roadmap |

---

## Хранение данных и recruiter cockpit

### БД

Три новые таблицы в `bot_admin`:

```sql
hr_candidates: id, name, telegram_id, email, source, role, status, created_at
hr_answers:    id, candidate_id, stage, question, answer_text, answer_media_url, ai_score, ai_analysis, created_at
hr_scores:     id, candidate_id, stage, criterion, score, reasoning, created_at
```

### UI (bot-admin-ui)

- `/hr/candidates` — список с фильтрами (стадия, роль, score, дата)
- `/hr/candidates/:id` — карточка: все ответы по всем этапам, AI-саммари, action buttons
- `/hr/interview/:token` — страница видеоинтервью для кандидата
- `/hr/dashboard` — воронка найма: отклик → квалификация → тестовое → видео → интервью → оффер, конверсии по каждому этапу

---

## Антипаттерны AI-воронки

1. **Внешние сервисы** (TestGorilla, Hireflix, Pymetrics) — дороже, хуже интеграция, данные не наши
2. **Живое интервью как 2-й шаг** — owner закопается в 30+ часах разговоров
3. **Сложные статические формы** вместо Telegram-бота — конверсия ниже, нет DISC
4. **Тестовое "напиши стратегию"** — банально, не фильтрует
5. **Вопросы из книги "Кто" в видеоинтервью** — работают для живого, на камере слишком жёсткие
6. **Запуск без калибровки AI** — **обязательный ритуал, не nice-to-have**: первые 10-15 кандидатов owner смотрит вручную и сравнивает с AI ДО любого массового отсева. Без калибровки скоринг будет давать ложноположительные на ступенях LLM (типичная картина: 5-7 кандидатов на одинаковом prescreen=72, неотличимые)
7. **Фильтр по «годам опыта» вместо сигналов из диалога** — отсекать senior'ов hard-reject'ом по `experience_years > 7` неверно для нишевых компаний (edtech/астрология). Senior, согласившийся на ширину вилки, может быть мотивирован уходом из корпората, а не «временно посидеть». Правильный фильтр overqualified — три сигнала ИЗ ДИАЛОГА: salary_high (>100K), wants_management (хочет руководить командой PMO), process_mismatch (раздражён отсутствием Jira/Asana/Agile). Эти три флага собираются в `dialog_quality_prompt` как отдельное поле, не как penalty к скорингу.
8. **score_dialog_quality без runtime-триггера** — функция определена, промпт хороший, но никто не вызывает после нового сообщения → dialog_quality_score=NULL у всех → composite-формула 0.3p+0.7d не работает → сортировка по сырому prescreen → ничьи на ступенях LLM. Подключение триггера обязательно ДО публикации вакансии (см. секцию Runtime-триггер ниже).
9. **composite_score не персистится в БД** — если в схеме только `composite_tier` (текстовое A/B/C) без числового `composite_score`, отсортировать список по композиту физически нельзя. Сначала миграция, потом скоринг.
10. **MCP/REST-tools агента без `role_key` параметра** — при найме параллельно ≥2 ролей tool без фильтра роли смешивает кандидатов разных вакансий. Промпт системы может говорить «фильтруй по project_manager», но физически нечем — schema без поля. Проверить контракты ВСЕХ tools агента до запуска параллельного найма.
11. **Зарплатные ожидания не извлекаются** — без `salary_expectation` в БД главный фильтр overqualified мёртв. Парсить regex'ом из любого ответа кандидата + обязательный явный вопрос про вилку после 3-4 ответа диалога.

---

## Когда НЕ использовать AI-воронку

- Для ролей МП и РОП — EXPRESS и FULL треки работают лучше (см. hiring-playbook.md)
- Если нет времени на построение инструмента (2-3 сессии разработки)
- Для массовых разовых задач (например, seasonal saleman)
- Для очень узких экспертов где рынок маленький — лучше headhunting через нетворкинг

---

## HH-приглашение: правила текста

### Правильный шаблон (бот: @hr_alexbot)
```
Здравствуйте! Посмотрели ваш профиль — интересный опыт.

Приглашаем на первый этап: знакомство с нашим AI-агентом.
Займёт ~10-15 минут, отвечать можно в удобном темпе.

Переходите: https://t.me/hr_alexbot?start=hh_{neg_id}
```

### Правила копирайтинга
- **Всегда приветствие** — без него выглядит как спам
- **"AI-агент"**, не "Telegram-бот" — не обесценивать инструмент
- **Принцип Зейгарник** — не раскрывать всё сразу (не говорить про "аналитическую задачу", "скрининг", этапы). Лёгкий вход → потом внутри бот раскрывает следующие шаги
- **Не "скрининг"** — слово создаёт ощущение отбора/проверки, снижает конверсию в переход

### Антипаттерны
- ❌ `короткий скрининг через Telegram-бота`
- ❌ `задаст вопросы и предложит аналитическую задачу`
- ❌ отсутствие приветствия
- ❌ неверный username бота

---

## Решения об отказе

### Кто принимает решения
**Всегда Алекс**, не автоматически:
- A/B кандидаты (высокий/средний скоринг) → уведомление с полным scorecard + кнопки `✅ Отправить тестовое | ❌ Не подходит`
- C кандидаты (низкий скоринг, FAIL) → авто-отказ кандидату + короткое уведомление Алексу
- Pre-screen C → авто-отказ через HH API (до входа в бот)

### HH rejection API (правильная последовательность)
```python
# НЕПРАВИЛЬНО — вернёт 403:
PUT /negotiations/discard/{neg_id}
PUT /negotiations/consider/{neg_id}

# ПРАВИЛЬНО — сначала перевести в phone_interview, потом discard:
PUT /negotiations/phone_interview/{neg_id}
PUT /negotiations/discard_by_employer/{neg_id}  # body: {"message": "текст отказа"}
```

---

## Технические уроки (бот @hr_alexbot)

### Критичные баги и фиксы

**Баг: null content от OpenRouter → crash**
- Симптом: `AttributeError: 'NoneType' has no attribute 'strip'` → бот отвечает "технические неполадки"
- Причина: Claude Sonnet через OpenRouter иногда возвращает `content: null` с `finish_reason: stop`
- Фикс: `_call_agent_text` ретраит 3 раза с exponential backoff; `_openrouter_call` логирует и бросает RuntimeError

**Баг: кандидат перезашёл по /start не ответив на последний вопрос**
- Симптом: history заканчивается на сообщении агента → передаём Claude conversation `[user, assistant, user, assistant]` → null content
- Фикс: если `messages[-1].role == "agent"` → сразу вернуть последний вопрос без API-вызова

**Баг: неверный username бота в приглашениях**
- Правильный: `@hr_alexbot` (не `@veda_hr_bot`)
- Константа `BOT_USERNAME = "hr_alexbot"` в `/root/hr-bot/bot.py`

### Деплой hr-bot
```bash
# На сервере нет git remote — только rsync:
rsync -az --exclude=venv/ --exclude='.git/' --exclude='__pycache__' \
  /Users/alex/Projects/platform/hr-bot/ root@109.73.194.217:/root/hr-bot/
ssh root@109.73.194.217 "pm2 restart hr-bot"
```

---

## Rescoring v2 — система оценки кандидатов (2026-04-19)

> Реализована в сессии найма маркетолога. Заменила v1 (только prescreen_score).

### Архитектура оценки

**Итоговый composite_tier** = взвешенная сумма двух этапов:
```
composite_score = prescreen_score × 0.3 + dialog_quality_score × 0.7
```

Тиры:
- **Tier A** — composite ≥ 70 (финалисты, показываем owner)
- **Tier B** — composite ≥ 50 (резерв)
- **Tier C** — composite < 50 (авто-отказ)

**Поля в `hr_candidates`** (все 4 добавлены в Rescoring v2):
- `composite_tier` — A/B/C
- `dialog_quality_score` — 0..100, итог DIALOG_QUALITY_PROMPT
- `resume_authenticity` — 0..100, оценка "написано самим или сгенерировано"
- `prescreen_evidence` — JSON с разбивкой по критериям

### Pre-screen скоринг (prescreen_score)

LLM оценивает резюме по критериям, возвращает `score` (0..100) и `verdict` (invite / pending / reject).

**Бонус за образование (deterministic, post-LLM):**
```python
TOP_UNIVERSITIES = [
    "МГУ", "ВШЭ", "МФТИ", "Бауман", "СПбГУ", "ИТМО", "НГУ",
    "РАНХиГС", "МГИМО", "Финансовый университет", "РЭУ Плеханова"
]
RELEVANT_SPECIALTIES = ["маркетинг", "менеджмент", "экономика", "финансы",
                         "математика", "статистика", "прикладная математика",
                         "информатика", "социология", "психология"]
```
- Топ-вуз + релевантная специальность → **+5 баллов**
- Физмат (любой вуз) → **+5 баллов**
- Оба условия одновременно → **+8 баллов** (не суммируются, берётся max)

Бонус применяется ПОСЛЕ LLM-оценки. Может перевернуть reject → invite если score пересекает 50.

**КРИТИЧНО:** Бонус за физмат = всегда, независимо от вуза. РАНХиГС (Академия Президента) = топ-вуз, релевантный для маркетинга/менеджмента.

### Dialog Quality скоринг (dialog_quality_score)

DIALOG_QUALITY_PROMPT оценивает переписку в HH по 5 критериям (сумма = 100 баллов):

| Критерий | Вес | Что оценивается |
|---|---|---|
| `ai_practical` | 20 | Практический опыт с AI-инструментами с реальным кейсом |
| `communication_quality` | 20 | Структура, ясность, конкретность ответов |
| `motivation_clarity` | 20 | Понятна ли мотивация, есть ли энергия |
| `professional_depth` | 20 | Глубина экспертизы в профессии |
| `growth_mindset` | 20 | Готовность учиться, примеры роста |

**Критерий ai_practical — строгие правила:**
```
- 20 баллов: кандидат назвал конкретный AI-инструмент + описал кейс + указал результат/эффект
- 10 баллов: упомянул AI-инструменты без конкретного кейса (общие слова)
- 0 баллов: AI вообще не упоминался ЛИБО только "пишу тексты в ChatGPT"

ВАЖНО: Если кандидат ни разу не упомянул AI — ai_practical = 0 строго.
```

В промпт также передаётся контекст вакансии (`vacancy_requirements`), чтобы LLM оценивал релевантность, а не только полноту ответов.

### Что значит "вау кандидат"

**Зелёные флаги (Tier A — финалист):**
- Конкретные AI-инструменты с результатом: "Notion AI для брифов, Claude для анализа ЦА, сократил подготовку к запуску с 3 дней до 4 часов"
- Цифры с контекстом: не "увеличил конверсию", а "OR вырос с 12% до 19% за 6 недель через сегментацию + A/B 3 вариантов"
- Самостоятельные инициативы: построил инструмент/автоматизацию без задания сверху
- AI как рабочий инструмент, не как факт наличия

**Красные флаги (не Tier A, даже при структурных ответах):**
- Нет AI-опыта вообще (или только "пишу тексты в ChatGPT") → ai_practical = 0 → composite падает на ~14 баллов
- Управленческие амбиции при найме IC: "хотелось бы управлять уже" → управленческий мисматч → дисквалификация
- Красиво пишет, но нет конкретики = Tier B максимум

**Кейс Якубаев Султан (2026-04-19):**
- prescreen=35 (слабое резюме), dialog=88 (старый промпт без ai_practical)
- LLM поставил 88 за структурированные ответы, хотя AI не упомянут ни разу
- После ai_practical фикса: пересчитан вручную до ~59, переведён в Tier B
- **Урок**: структурные ответы без AI в AI-first роли = Tier B, не Tier A

**Кейс Бондарь Наталья (2026-04-19):**
- Tier A #4, NDA-защищённый опыт в AI-компании (AAIC, Midjourney, ML recommender)
- Бот дал ошибочную рекомендацию "пропустить" — игнорировал AI-опыт, видел только "боты не писала"
- Статус: test_sent (правильно, уже в пайплайне)
- **Урок**: NDA-опыт = засчитывается; "не писала ботов" ≠ "нет AI-опыта"

---

## Runtime-триггер dialog_quality (2026-04-27)

**Когда запускать `score_dialog_quality()`** — после каждого нового сообщения кандидата, если:
```python
candidate_msgs = [m for m in messages if m.role == "candidate"]
count = len(candidate_msgs)
if count >= 4 and count % 2 == 0:
    asyncio.create_task(_async_rescore_dialog_quality(candidate_id))
```

То есть: первый запуск после 4-го сообщения, потом каждые 2 новых (6, 8, 10…). Это даёт 4-5 пересчётов за полный диалог.

**Защита от гонок** — `asyncio.Lock` per `candidate_id` (модуль-уровневый dict). Если lock занят — пропустить запуск, следующий вызов всё равно перепосчитает.

**Что обновлять при каждом запуске**:
- `dialog_quality_score` (0-100)
- `composite_score` (через `compute_tier()`, новый возврат tuple)
- `composite_tier` (A/B/C по role-specific порогам)
- `agent_state["pm_flags"]` если LLM вернул `flags` (salary_high, wants_management, process_mismatch)

**Salary parsing** — параллельно с триггером, regex на каждое сообщение кандидата:
```python
_SALARY_RE = re.compile(
    r"(?<![\d,])(\d{2,3})\s*(?:к\b|К\b|тыс|тысяч)|"
    r"(?<![\d,])(\d{2,3})\s?000\s*(?:руб|₽|р\.?)?",
    re.IGNORECASE
)
# Парсит "80К", "100 тыс", "150к на руки", "от 90 000"
# Игнорирует <20K и >500K (нереалистично)
# Пишет в hr_candidates.salary_expectation если NULL (не перезаписывать)
```

**Bulk-rescoring legacy кандидатов** — после подключения runtime-триггера разово запустить `python scripts/rescore_all_candidates.py` для всех `prescreen_verdict='invite'`. Скрипт идемпотентный, пропускает уже посчитанных. Стоимость: ~$10-30 на 900 кандидатов (Haiku).

## Анти-паттерн «фильтр овквалификации по годам» (2026-04-27)

❌ **Не делать**: hard-reject в `prescreen_prompt` при `experience_years > 7` или подобный фильтр в SQL-выгрузках по сырому полю.

**Почему**: в нишевых компаниях с малой ЗП (edtech, регионы, узкие вертикали) senior соглашается на 60-80К по двум валидным причинам:
- ушёл с корпората, не выгорает (хочет «делать руками», не руководить);
- ниша интересна (астрология / edtech / малый бизнес — для определённой аудитории это ценность сама по себе).

**Что делать** — ловить три сигнала ИЗ ДИАЛОГА в `dialog_quality_prompt`:
1. `salary_high` — кандидат назвал ожидания > 100К на руки или эквивалент. Прямой парсинг + LLM-флаг.
2. `wants_management` — ищет head-of / руководителя отдела / build-команду PM-ов (НЕ IC-роль).
3. `process_mismatch` — раздражённое ожидание корпоративных процессов («у вас Jira нет?», «как вы без спринтов?»).

Флаги хранятся в `agent_state["pm_flags"]` (jsonb), не как penalty к dialog_quality_score. Шорт-лист считается так:
```sql
WHERE composite_score >= 60
  AND role_key = 'project_manager'
  AND status NOT IN ('rejected', 'hired')
  AND (salary_expectation IS NULL OR salary_expectation <= 100000)
  AND (agent_state->'pm_flags'->>'wants_management' IS NULL
       OR agent_state->'pm_flags'->>'wants_management' = 'false')
```

Senior с опытом 8 лет и `salary_expectation=80000` без management-флага — рабочий шорт-лист. Junior с опытом 1 год и management-амбициями — нет.

## Операционные правила hr-bot (2026-04-19)

### Отклонённые кандидаты

**Правило:** дисквалифицированный кандидат ДОЛЖЕН иметь `status = 'rejected'` в `hr_candidates`.

Endpoint `GET /hr/tier-summary` фильтрует: `WHERE status != 'rejected'`. Без этого отклонённые попадают в список Tier A.

```sql
-- Ручное отклонение:
UPDATE hr_candidates SET status = 'rejected' WHERE id = <id>;
```

**Критерии для отклонения:**
- Управленческий мисматч (ищет управление, нанимаем IC)
- Явный мисматч по роли
- Неадекватное поведение в диалоге

### Tier-summary vs pipeline-stats

Два разных источника данных в боте:
- `GET /hr/tier-summary` — корректный источник, только prescreen_verdict='invite' + status!='rejected', с dialog_quality
- `GET /hr/pipeline-stats` — агрегат всего, включая все rescored кандидаты без фильтра отклонённых

Если tier-summary отдаёт 500 → бот автоматически падает на pipeline-stats → цифры раздуваются (был случай 95 Tier A вместо 18).

**При любых аномальных цифрах** — первым делом проверить `GET /hr/tier-summary` напрямую.

### Синхронизация моделей сервер/локально

**КРИТИЧНО:** файлы моделей SQLAlchemy на сервере и локально должны быть синхронизированы.

Если добавлены новые поля в локальный `hr_candidate.py` → обязательно rsync на сервер:
```bash
rsync -az platform/bot-admin-api/app/models/hr_candidate.py \
  root@109.73.194.217:/root/bot-admin-api/app/models/hr_candidate.py
ssh root@109.73.194.217 "cd /root/bot-admin-api && pm2 restart bot-admin-api"
```

Признак рассинхрона: `AttributeError: 'HrCandidate' object has no attribute 'composite_tier'` в логах.

---

## Vision-поддержка в hr-bot (2026-04-19)

HR-бот поддерживает анализ скриншотов от рекрутера (RECRUITER_CHAT_ID = 814865188).

### Как работает

1. Рекрутер присылает фото (скриншот переписки HH, карточки кандидата и т.д.)
2. Бот скачивает максимальный размер (`message.photo[-1]`)
3. Кодирует в base64
4. Определяет контекст кандидата (из reply или последнего активного)
5. Отправляет в Claude Haiku через OpenRouter как `image_url` content block
6. Отвечает анализом в чат

### Код vision-вызова
```python
async def _ask_ai_vision(system_prompt, question, image_base64, mime_type="image/jpeg"):
    payload = {
        "model": "anthropic/claude-haiku-4-5",
        "messages": [{
            "role": "user",
            "content": [
                {"type": "image_url",
                 "image_url": {"url": f"data:{mime_type};base64,{image_base64}"}},
                {"type": "text", "text": question}
            ]
        }],
        "max_tokens": 800
    }
    # HTTP timeout: 45s, через OPENROUTER_API_KEY
```

### Важно: aiogram F.photo handler

Handler для фото ДОЛЖЕН быть отдельным (не через F.text):
```python
@router.message(F.from_user.id == RECRUITER_CHAT_ID, F.photo)
async def handle_recruiter_photo(message: Message): ...
```

Без этого handler photo-сообщения проваливаются как "not handled" — бот не отвечает, хотя на текст отвечает нормально. Это НЕ баг авторизации, это отсутствующий handler.

### Диалог с диалогом

Когда hr_active_dialogs entry удалена (статус test_sent) → бот перестаёт опрашивать HH для этого кандидата. Это ожидаемое поведение, не баг.

---

## Референс-реализация

Полный roadmap реализации AI-воронки для найма Growth-маркетолога Vedantica:
`/Users/alex/Projects/platform/specs/ROADMAP-marketer-hiring-2026-04-04.md`

Там есть:
- KPI и профиль роли
- 7 сессий с handoff-ами (Research → Funnel Design → AI Agent Build → Launch → Calibration → Offer → 30/60/90)
- Гипотезы для проверки
- Стратегические развилки
